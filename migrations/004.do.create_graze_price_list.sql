CREATE TABLE price_list (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    product_id INTEGER
        REFERENCES products(id) ON DELETE RESTRICT NOT NULL,
    vendor_id INTEGER
        REFERENCES vendors(id) ON DELETE RESTRICT NOT NULL,
    price NUMERIC NOT NULL,
    units TEXT NOT NULL
);